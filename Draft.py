# -*- coding: utf-8 -*-

#y = df.iloc[0:int(sum(n_inst)), c_classes].values
#print(list(set(y)))
#
#
#                 y=[0]*sum(self.n_inst)
#                 for k in range(SI,sum(self.n_inst[0:i])):
#                     y[k]=1
#                 y = np.where(y == 0, -1,1)

x= 98
print(round(x*0.25))